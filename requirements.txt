# --- Web server ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9      # file uploads

# --- LangChain (split packages, v1.x ecosystem) ---
langchain>=1.0
langchain-core>=1.0
langchain-text-splitters>=0.3
langchain-huggingface>=0.1   # local embeddings
langchain-chroma>=0.2        # local vector store

# --- LLM providers for the GENERATE step (you only need the one you use) ---
langchain-anthropic>=0.3
langchain-openai>=0.3
langchain-ollama>=0.3

# --- Embeddings + vector store engines ---
sentence-transformers>=3.0   # pulls in torch; ~first run downloads the model
chromadb>=0.5

# --- Document loading ---
pypdf>=5.0

# --- Config ---
python-dotenv>=1.0
